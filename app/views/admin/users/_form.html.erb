<%= semantic_form_for [:admin, @user] do |f| %>
	<%= f.inputs do %>
		
		<%= f.input :account_id, as: :select, collection: Account.all %>		
		
	<% end %>
	<%= f.actions do %>
		<%= f.action :submit %>
	<% end %>
<% end %>