<%= form_for(@brand) do |f| %>
	<%= render "shared/error_msg", object: f.object %>

	<%= f.label :name, "Add new brand." %>
	<%= f.text_field :name %>

	<%= f.submit "Add", class: "btn btn-lg btn-primary" %>
<% end %>