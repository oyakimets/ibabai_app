<% provide(:title, current_user.name) %>
<div class="row">
	<aside class="col-md-4">
		<section>
			<%= render "shared/user_info" %>
		</section>
		<section class="brand-form">
			<%= render "shared/brand_form" %>
		</section>
	</aside>
	<div class="col-md-8" id="my-brands">
		<h3>My brands</h3>
		<%= render 'shared/list' %>
	</div>
</div>